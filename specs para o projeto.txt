

requer gradle 7.6

criando a rede docker necessária:

docker network create -d bridge <nome_rede>

para compilar o auditor:
 - sudo docker build . -t auditor --build-arg PORT=<port>
para compilar o jogador :
 - sudo docker build . -t jogador --build-arg NAME=<nome_jogador> --build-arg PORT=<port>

para rodar o auditor
 - sudo docker run --network <rede> auditor
para rodar o jogador
 - sudo docker run --network <rede> jogador


nao consegui usar o docker compose ainda

